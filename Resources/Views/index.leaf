#extend("master")

#export("head") {
    #embed("welcome")
}

#export("body") {
    #loop(user, "user") {
        <h1>Hello, #(user.name)! <a href="mailto:#(user.email)">[Email]</a></h1>
    }
}



<!DOCTYPE html>
<html>
    <head>
        #import("head")
    </head>
    <body>

            </body>
</html>
